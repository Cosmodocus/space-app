/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images-assets.nasa.gov', 'via.placeholder.com'] // Add other domains if necessary
  }
};

module.exports = nextConfig;
